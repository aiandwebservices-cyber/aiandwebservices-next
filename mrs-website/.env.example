PIPEDRIVE_API_TOKEN=
PIPEDRIVE_COMPANY_DOMAIN=aiandwebservices
RESEND_API_KEY=

# Google Analytics 4 — public env var (exposed to the browser).
# TODO(David): create a GA4 property at https://analytics.google.com,
# copy its measurement ID (format G-XXXXXXXXXX), and set this in Vercel.
# When unset, no GA script is rendered and trackEvent() becomes a no-op.
NEXT_PUBLIC_GA_ID=

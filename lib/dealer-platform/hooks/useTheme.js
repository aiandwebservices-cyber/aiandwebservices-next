// Re-export the useTheme hook from ThemeProvider for convenient imports.
// Components can do: import { useTheme } from '@/lib/dealer-platform/hooks/useTheme';
export { useTheme, ThemeProvider } from '../theme/ThemeProvider';
